name: Build and Optimize Contract

on:
  workflow_call:
    inputs:
      contract_dir:
        description: 'Relative path to the contract directory'
        type: string
        required: false
      release_name: 
        description: 'Name for the release'
        required: true
        type: string
      release_description:
        description: 'Description for the release'
        required: false
        type: string
    secrets:
      release_token:
        description: 'Github token'
        required: true

jobs:
  build:
    runs-on: ubuntu-latest 
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run docker container
        run: docker run --rm -e CONTRACT_DIR=${{ inputs.contract_dir }} -v "${{ github.workspace }}:/inspector/home" hawthorne/contract-build:latest

      - name: Get wasm file name
        run: |
          cd ${{ github.workspace }}/release
          wasm_file=$(find -type f -name "*.wasm")
          echo "WASM_FILE_NAME=$(basename $wasm_file)" >> $GITHUB_ENV  
          echo "WASM_FILE_SHA256=$(sha256sum $wasm_file | cut -d ' ' -f 1)" >> $GITHUB_ENV 

      - name: Create release ${{ env.WASM_FILE_SHA256 }}
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.release_token }}
        with:
          tag_name: ${{ inputs.release_name }}
          release_name: ${{ inputs.release_name }}
          draft: false
          prerelease: false
          body: ${{ inputs.release_description }}
  
      - name: Upload wasm to release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.release_token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ${{ github.workspace }}/release/${{ env.WASM_FILE_NAME }}
          asset_name: ${{ env.WASM_FILE_NAME }}
          asset_content_type: application/wasm